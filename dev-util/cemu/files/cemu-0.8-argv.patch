diff --git a/src/cemu/__main__.py b/src/cemu/__main__.py
index f257840..fc6e1bd 100644
--- a/src/cemu/__main__.py
+++ b/src/cemu/__main__.py
@@ -3,6 +3,7 @@
 import argparse
 import pathlib
 import os
+import sys
 
 import cemu.const
 import cemu.core
@@ -19,13 +20,13 @@ def setup_remote_debug(port: int = cemu.const.DEBUG_DEBUGPY_PORT):
     cemu.log.dbg("Client connected, resuming session")
 
 
-def main(argv: list[str]):
+def main():
     parser = argparse.ArgumentParser(prog=cemu.const.PROGNAME, description=cemu.const.DESCRIPTION)
     parser.add_argument("filename")
     parser.add_argument("--debug", action="store_true")
     parser.add_argument("--attach", action="store_true")
     parser.add_argument("--cli", action="store_true")
-    args = parser.parse_args(argv)
+    args = parser.parse_args(sys.argv)
 
     if bool(os.getenv("DEBUG", False)) or args.debug:
         cemu.const.DEBUG = True
@@ -43,8 +44,6 @@ def main(argv: list[str]):
 
 
 if __name__ == "__main__":
-    import sys
-
     path = pathlib.Path(__file__).absolute().parent.parent
     sys.path.append(str(path))
-    main(sys.argv)
+    main()
